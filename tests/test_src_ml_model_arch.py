import pytest
from unittest.mock import MagicMock, patch
from src.ml.model_arch import build_lstm_attention_model, LSTMAttentionLayer, AttentionLayer


@pytest.fixture
def mock_tf():
    with patch.dict('sys.modules', {'tensorflow': MagicMock(), 'keras': MagicMock(), 'tf': MagicMock()}):
        import tensorflow as tf
        tf.keras = MagicMock()
        tf.keras.layers = MagicMock()
        tf.keras.Model = MagicMock()
        tf.keras.Input = MagicMock()
        tf.keras.layers.LSTM = MagicMock()
        tf.keras.layers.Dense = MagicMock()
        tf.keras.layers.Concatenate = MagicMock()
        tf.keras.layers.Attention = MagicMock()
        tf.keras.layers.TimeDistributed = MagicMock()
        tf.keras.layers.GlobalAveragePooling1D = MagicMock()
        tf.keras.backend = MagicMock()
        tf.keras.backend.int_shape = MagicMock(return_value=(None, 10, 64))
        tf.random = MagicMock()
        tf.random.normal = MagicMock(return_value=MagicMock())
        tf.ones = MagicMock(return_value=MagicMock())
        tf.zeros = MagicMock(return_value=MagicMock())
        tf.keras.initializers = MagicMock()
        tf.keras.initializers.Ones = MagicMock(return_value=MagicMock())
        tf.keras.initializers.Zeros = MagicMock(return_value=MagicMock())
        tf.keras.constraints = MagicMock()
        tf.keras.constraints.UnitNorm = MagicMock(return_value=MagicMock())
        tf.keras.regularizers = MagicMock()
        tf.keras.regularizers.l2 = MagicMock(return_value=MagicMock())
        tf.keras.activations = MagicMock()
        tf.keras.activations.softmax = MagicMock()
        tf.keras.backend.dot = MagicMock()
        tf.keras.backend.concatenate = MagicMock()
        tf.keras.backend.expand_dims = MagicMock()
        tf.keras.backend.tile = MagicMock()
        tf.keras.backend.permute_dimensions = MagicMock()
        tf.keras.backend.reshape = MagicMock()
        tf.keras.backend.sum = MagicMock()
        tf.keras.backend.mean = MagicMock()
        tf.keras.backend.shape = MagicMock(return_value=MagicMock())
        tf.keras.backend.cast = MagicMock()
        tf.keras.backend.floatx = MagicMock(return_value='float32')
        tf.keras.backend.epsilon = MagicMock(return_value=1e-7)
        tf.keras.backend.get_value = MagicMock(return_value=0.1)
        tf.keras.backend.constant = MagicMock(return_value=MagicMock())
        tf.keras.backend.variable = MagicMock(return_value=MagicMock())
        tf.keras.backend.ones_like = MagicMock(return_value=MagicMock())
        tf.keras.backend.zeros_like = MagicMock(return_value=MagicMock())
        tf.keras.backend.abs = MagicMock()
        tf.keras.backend.exp = MagicMock()
        tf.keras.backend.log = MagicMock()
        tf.keras.backend.sigmoid = MagicMock()
        tf.keras.backend.tanh = MagicMock()
        tf.keras.backend.relu = MagicMock()
        tf.keras.backend.softmax = MagicMock()
        tf.keras.backend.mean = MagicMock()
        tf.keras.backend.sum = MagicMock()
        tf.keras.backend.max = MagicMock()
        tf.keras.backend.min = MagicMock()
        tf.keras.backend.stack = MagicMock()
        tf.keras.backend.unstack = MagicMock()
        tf.keras.backend.tile = MagicMock()
        tf.keras.backend.repeat_elements = MagicMock()
        tf.keras.backend.slice = MagicMock()
        tf.keras.backend.gather = MagicMock()
        tf.keras.backend.shape = MagicMock(return_value=MagicMock())
        tf.keras.backend.int_shape = MagicMock(return_value=(None, 10, 64))
        tf.keras.backend.cast = MagicMock()
        tf.keras.backend.concatenate = MagicMock()
        tf.keras.backend.expand_dims = MagicMock()
        tf.keras.backend.permute_dimensions = MagicMock()
        tf.keras.backend.reshape = MagicMock()
        tf.keras.backend.dot = MagicMock()
        tf.keras.backend.transpose = MagicMock()
        tf.keras.backend.batch_dot = MagicMock()
        tf.keras.backend.relu = MagicMock()
        tf.keras.backend.sigmoid = MagicMock()
        tf.keras.backend.tanh = MagicMock()
        tf.keras.backend.softmax = MagicMock()
        tf.keras.backend.softplus = MagicMock()
        tf.keras.backend.softsign = MagicMock()
        tf.keras.backend.elu = MagicMock()
        tf.keras.backend.selu = MagicMock()
        tf.keras.backend.gelu = MagicMock()
        tf.keras.backend.swish = MagicMock()
        tf.keras.backend.mish = MagicMock()
        tf.keras.backend.get_uid = MagicMock(return_value='1')
        tf.keras.backend.clear_session = MagicMock()
        tf.keras.backend.set_floatx = MagicMock()
        tf.keras.backend.set_epsilon = MagicMock()
        tf.keras.backend.get_session = MagicMock()
        tf.config = MagicMock()
        tf.config.experimental = MagicMock()
        tf.config.optimizer = MagicMock()
        tf.config.optimizer.get_experimental_options = MagicMock(return_value={})
        tf.config.optimizer.set_experimental_options = MagicMock()
        tf.data = MagicMock()
        tf.data.Dataset = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data.Dataset.prefetch = MagicMock()
        tf.data.Dataset.map = MagicMock()
        tf.data.Dataset.filter = MagicMock()
        tf.data.Dataset.enumerate = MagicMock()
        tf.data.Dataset.zip = MagicMock()
        tf.data.Dataset.from_tensors = MagicMock()
        tf.data.Dataset.range = MagicMock()
        tf.data.Dataset.from_generator = MagicMock()
        tf.data.Dataset.from_tensor_slices = MagicMock()
        tf.data.Dataset.interleave = MagicMock()
        tf.data.Dataset.flat_map = MagicMock()
        tf.data.Dataset.window = MagicMock()
        tf.data.Dataset.padded_batch = MagicMock()
        tf.data.Dataset.shuffle = MagicMock()
        tf.data.Dataset.repeat = MagicMock()
        tf.data.Dataset.take = MagicMock()
        tf.data.Dataset.skip = MagicMock()
        tf.data.Dataset.cache = MagicMock()
        tf.data